name: app_example
description: A new Flutter project used for example purpose.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.0
  mobx: ^2.0.0
  flutter_mobx: ^2.0.0
  flutter_modular: ^3.3.1

dev_dependencies:
  # flutter_native_splash: ^1.2.0 # flutter pub run flutter_native_splash:create
  # test: # for unit test
  flutter_test: # for widget test
    sdk: flutter
  # flutter_driver: # for integration test
    # sdk: flutter
  build_runner: ^1.12.2
  mobx_codegen: ^2.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/icons/
    - assets/images/
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  # 
  # flutter packages pub run build_runner build --delete-conflicting-outputs
  # flutter packages pub run build_runner watch --delete-conflicting-outputs 
